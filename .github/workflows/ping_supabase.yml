name: Ping Supabase to Prevent Pausing

on:
  schedule:
    - cron: '5 14 * * 0'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl https://internal.taebforassembly.com/view/greeting
